name: AI SR SKILLS

on:
  pull_request_target:
    branches:
      - main
      - "branch*"
    types:
      - opened
      - reopened

concurrency:
  group: AI-SR-SKILLS-${{ github.event.number }}
  cancel-in-progress: true

permissions:
  pull-requests: write

jobs:
  assign-reviewer:
    runs-on: [self-hosted, normal]
    continue-on-error: true
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
    if: >
      github.event.action == 'opened' &&
      !startsWith(github.head_ref, 'mergify/')
    steps:
      - name: Debug token
        run: |
          if [ -n "${CLAUDE_CODE_OAUTH_TOKEN}" ]; then
            echo "CLAUDE_CODE_OAUTH_TOKEN is set, length=${#CLAUDE_CODE_OAUTH_TOKEN}"
          else
            echo "CLAUDE_CODE_OAUTH_TOKEN is empty or not set"
          fi
      - name: Assign PR Reviewer
        run: |
          /var/local/env/tools/claude -p "/starrocks-assign-pr-reviewer https://github.com/${{ github.repository }}/pull/${{ github.event.number }}" || true

